import { EllipsisTipDirective } from './ellipsis-tip.directive';

describe('EllipsisTipDirective', () => {
  it('should create an instance', () => {
    const directive = new EllipsisTipDirective();
    expect(directive).toBeTruthy();
  });
});
