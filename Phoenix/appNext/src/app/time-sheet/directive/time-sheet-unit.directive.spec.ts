import { TimeSheetUnitDirective } from './time-sheet-unit.directive';

describe('TimeSheetUnitDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeSheetUnitDirective();
    expect(directive).toBeTruthy();
  });
});
